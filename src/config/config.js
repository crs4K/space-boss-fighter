require.config({
  baseUrl: "src",
  paths: {
    Phaser: "../node_modules/phaser/build/phaser.min",
    component: "component",
    constant: "constant",
    controller: "controller",
    signal: "signal",
    state: "state"
  }
});